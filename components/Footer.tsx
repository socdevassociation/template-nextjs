import { Footer as MantineFooter, Group } from "@mantine/core";

export default function SDAFooter() {
  return (
    <MantineFooter height={60} p="md">
      <Group>
        <span>Footer</span>
      </Group>
    </MantineFooter>
  );
}
